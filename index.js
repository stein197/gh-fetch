/**
 * @typedef {{
 * 	user: string;
 * 	auth: string;
 * }} Options
 */

// @ts-check

/**
 * @param {string} type
 * @param {Options} opts
 * @returns {Promise}
 */
module.exports = function (type, opts) {}
